apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-metrics
spec:
  type: NodePort
  ports:
    - name: port
      port: 9090
      targetPort: 9090
      nodePort: {{ .Values.metrics.service.nodePort }}
  selector:
    app.kubernetes.io/name: prometheus
